# ============================================================
# ShowTail — полный список зависимостей
# ============================================================
# Для офлайн-установки: scripts/download_packages.sh
# ============================================================

# --- Этап 1: Фундамент ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# --- Этап 2: Авторизация и безопасность ---
passlib[bcrypt]>=1.7.4
bcrypt<4.1                      # passlib 1.7.4 несовместим с bcrypt>=4.1 (ломает verify: "password > 72 bytes")
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.12
redis[hiredis]>=5.2.0           # Progressive rate limiting, кэш, WS pub/sub

# --- Этап 4: Файлы (MinIO S3) ---
boto3>=1.35.0                   # S3 клиент для MinIO
aioboto3>=13.0.0                # Async обёртка для boto3
Pillow>=11.0.0                  # Валидация/ресайз изображений
aiofiles>=24.1.0                # Async файловые операции
python-magic>=0.4.27            # Magic bytes определение типов файлов (libmagic)

# --- Этап 8: RabbitMQ + PDF ---
aio-pika>=9.5.0                 # RabbitMQ async клиент
reportlab>=4.2.0                # Программная генерация PDF
docxtpl>=0.18.0                # DOCX-шаблоны с Jinja-плейсхолдерами (официальные документы РКФ)
# weasyprint>=62.0              # HTML→PDF (альтернатива reportlab, тяжёлые системные зависимости)

# --- Этап 9: Email + Scheduler ---
aiosmtplib>=3.0.0               # Async SMTP
Jinja2>=3.1.0                   # Шаблоны email
APScheduler>=3.10.0             # Планировщик (cron-like задачи)

# --- Этап 11: WebSocket ---
websockets>=13.0                # Уже в uvicorn[standard], явная зависимость

# --- Этап 14: Production ---
structlog>=24.4.0               # Structured logging (JSON)

# --- Общие утилиты ---
email-validator>=2.0.0          # Валидация email (используется Pydantic)
httpx>=0.27.0                   # Async HTTP-клиент (worker book_handler — внешние API; рантайм, не только тесты)
bleach>=6.0.0                   # Санитизация HTML/XSS (app/middleware/sanitization.py)
